# Each example is one directory with one main.cpp: a complete, runnable
# recipe demonstrating a single rpgkit pattern. Keep them copy-adaptable —
# new examples start as a copy of the closest existing one.
add_subdirectory(chain_basics)
add_subdirectory(tutorials)
