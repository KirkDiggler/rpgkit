name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-linux:
    name: linux (${{ matrix.cxx }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cxx: [clang++, g++]
    env:
      CXX: ${{ matrix.cxx }}
    steps:
      - uses: actions/checkout@v4
      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build clang
      - name: Configure
        run: cmake --preset debug
      - name: Build
        run: cmake --build --preset debug
      - name: Test
        run: ctest --preset debug --output-on-failure

  build-windows:
    name: windows (MSVC)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -B build -DRPGKIT_BUILD_TESTS=ON
      - name: Build
        run: cmake --build build --config Debug
      - name: Test
        run: ctest --test-dir build -C Debug --output-on-failure

  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build clang clang-format clang-tidy
      # `make lint` = fmt-check + tidy; tidy depends on configure, which
      # produces build/debug/compile_commands.json for run-clang-tidy —
      # CI enforces exactly what `make pre-commit` does locally.
      - name: Lint
        run: make lint

  # Auto-tags AND publishes a GitHub Release when the version in CMakeLists.txt
  # changes and merges to main. Runs only on main pushes (not PRs), after
  # build/test/lint pass — so every release is green-gated. The tag is derived
  # from project(VERSION), so tag <-> version can't drift (no separate check
  # needed). Consumers (rpgkit-ue) pin FetchContent to these tags / Releases.
  # See issue #57.
  tag:
    name: tag release
    runs-on: ubuntu-latest
    needs: [build-linux, build-windows, lint]
    if: github.event_name == 'push'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Read version from CMakeLists.txt
        id: version
        run: |
          version=$(grep -oP 'project\(rpgkit VERSION \K[0-9]+\.[0-9]+\.[0-9]+' CMakeLists.txt)
          if [ -z "$version" ]; then
            echo "Failed to parse version from CMakeLists.txt" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"
      - name: Check if tag exists on remote
        id: check
        run: |
          if git ls-remote --exit-code --tags origin "refs/tags/${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Create tag
        if: steps.check.outputs.exists == 'false'
        run: |
          git tag ${{ steps.version.outputs.tag }}
          git push origin ${{ steps.version.outputs.tag }}
      # Gate the Release on the *Release* missing, not the tag, so the job
      # self-heals: if the tag was cut but `gh release create` later failed
      # (transient API error / rate limit), a subsequent run still publishes the
      # missing Release instead of skipping it forever. A no-op push where the
      # Release already exists is a clean skip, so we never re-release.
      - name: Check if release exists
        id: release_check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      # Publish in the same green-gated job that cut the tag (job already needs
      # build/test/lint). --verify-tag refuses to invent a tag if it isn't
      # present. This lives in the tag job rather than a separate tag-triggered
      # release.yml because a tag pushed with GITHUB_TOKEN can't trigger another
      # workflow run (GitHub's anti-recursion rule).
      - name: Publish GitHub Release
        if: steps.release_check.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            --title "${{ steps.version.outputs.tag }}" \
            --generate-notes \
            --verify-tag
